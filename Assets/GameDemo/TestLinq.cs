using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using UniRx;
public class TestLinq : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var value = 0;

        //print(Currency.Coin.ToString());
        print(((Currency)value).ToString());
    }
    void StasrtTestEnum(){

    }
}
public class ActorTest
{
    public string ID;
    public byte uid;
}
